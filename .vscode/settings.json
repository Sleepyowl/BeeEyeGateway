{
  "terminal.integrated.profiles.linux": {
    "zephyr": {
      "path": "/bin/bash",
      "args": [
        "-c",
        "source $ZEPHYR_BASE/../.venv/bin/activate && export BOARD_ROOT=${workspaceFolder} && exec bash"
      ]
    }
  },
  "terminal.integrated.defaultProfile.linux": "zephyr"
}
